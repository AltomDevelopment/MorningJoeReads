@using System.Web.UI.HtmlControls
@model MorningJoeReadsWebUI.ViewModel.SignUpViewModel
@{
    ViewBag.Title = "SignUp";
}
@Styles.Render("~/Content/LandingPage.css")
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device.width" />
</head>
<body>
    @if (Session["EmailAddress"] == null)
    {
        <h2>If you don't have an account you can create one!</h2>
    }
    <h2>Please enter your Information</h2>
    @using (Html.BeginForm("SignUp","SignUp", FormMethod.Post))
    {
        <table cellpadding="10" cellspacing="10" align="center">
            <tr>
                <th colspan="2" align="center">Login</th>
            </tr>
            <tr>
                <td style="color:antiquewhite">First Name:</td>
                <td>
                    @Html.TextBoxFor(m => m.FirstName)
                </td>
            </tr>
            <tr>
                <td style="color:antiquewhite">Last Name:</td>
                <td>
                    @Html.TextBoxFor(m => m.LastName)
                </td>
            </tr>
            <tr>
                <td style="color:antiquewhite">Email Address:</td>
                <td>
                    @Html.TextBoxFor(m => m.EmailAddress)
                </td>
            </tr>
            <tr>
                <td style="color:antiquewhite">Password:</td>
                <td>
                    @Html.TextBoxFor(m => m.PassWord)
                </td>
            </tr>
            <tr>
                <td></td>
                <td><input type="submit" value="Submit" /></td>
            </tr>
        </table>
    }
</body>
</html>
